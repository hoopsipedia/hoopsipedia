# OS files
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/

# Environment
.env
.env.local
.dev.vars
node_modules/

# Compressed backups (tracked originals exist)
*.gz
# ...except the box-score master, which is committed ONLY as gzip: the raw
# 61MB JSON exceeds Cloudflare Pages' 25MiB per-file limit and would fail
# every deploy (it did, silently, from 2026-07-24 to 2026-09-11).
# Local working copy: python3 scripts/pack_store.py --unpack
!sr_boxscores.json.gz
sr_boxscores.json

# Large source file (split into games_1.json + games_2.json for deploy)
games.json

# Wrangler
.wrangler/

# Claude Code
.claude/

# Local backups
backups/

# Archived code at repo root (preserved in git history; excluded so it doesn't deploy)
/archive/

# Runtime logs
logs/
*.log
sync_log.txt
boxscore_scrape_log.txt
scrape_chain_log.txt
scrape_log_batch1.txt

# Intermediate scrape data
scraped_boxscores_raw.json
boxscore_urls.json
games_to_scrape.json
_scrape_interim_*.json
_sr_audit.json
game_ids_progress.json
archives/**/*.pdf
archives/**/*.zip
